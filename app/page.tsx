import SkillSwapLanding from "@/components/Landing";


export default async function Home() {

  return (
    <div className="pt-24">
      <SkillSwapLanding/>
      
    </div>
  );
}

//Ttitle  changing for rach page ..
//the Script Component is used to load external scripts, such as Google Analytics or other third-party libraries, in a Next.js application. It allows you to specify when the script should be loaded (e.g., before the page is interactive) and ensures that it does not block the rendering of the page.